import { CreateMeetingDirective } from './create-meeting.directive';

describe('CreateMeetingDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateMeetingDirective();
    expect(directive).toBeTruthy();
  });
});
